# 公网部署方案：让各地用户填写并同步到你的数据库

## 一、当前架构简要说明

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + Vite | 开发时 `localhost:5173`，生产 build 后由后端托管 `frontend/dist` |
| 后端 | Node.js + Express | 监听 `0.0.0.0:3000`，提供 `/api/*`，并托管前端静态文件 |
| 数据库 | sql.js + SQLite | 单文件 `backend/data/experiment.db`，所有被试/专家/主试数据都在这里 |
| API 调用 | 相对路径 `/api/...` | 前端用 `fetch('/api/...')`，部署到同一域名下无需改代码 |

**现状问题**：服务只跑在你本机（或局域网），链接是 `http://localhost:3000/...`，外地用户、其他设备、其他浏览器无法访问；数据也只存在你本地的 `experiment.db` 里。

**目标**：把链接发出去后，各地、各设备、各浏览器都能打开并填写，提交的数据都写入**同一份**数据库，你在管理端能保存和查看。

---

## 二、结论：需要部署到公网可访问的环境

要让“别人能访问 + 数据都进你的库”，必须满足：

1. **前端和 API 有一个公网可访问的地址**（如 `https://your-domain.com`），而不是 `localhost`。
2. **后端和数据库跑在一台 7×24 在线的机器上**，所有用户请求都打到这台机器，数据写入这台机器上的 `experiment.db`。

因此，**需要把当前应用部署到云服务器（或带持久化的 PaaS）**。你本地可以继续用来开发，但“对外发放的链接”应指向云上的地址。

---

## 三、推荐方案对比

### 方案 A：单机云服务器（VPS）——**最推荐**

- **做法**：买一台云服务器（阿里云 / 腾讯云 / 华为云 / 国外 Vultr、DigitalOcean 等），把现有 **backend + frontend(dist)** 部署上去，用 Nginx 反代并配置 HTTPS，用 PM2 保活 Node 进程。
- **数据库**：继续用当前 sql.js + SQLite，`experiment.db` 放在服务器磁盘上即可。
- **优点**：
  - 和你现有架构完全一致，无需改数据库或后端逻辑。
  - 数据完全在你自己的机器上，备份、导出、查看都简单（SSH 上去或通过管理后台）。
  - 成本可控：1 核 2G 轻量服务器约几十元/月即可。
- **缺点**：需要自己维护一台服务器（装 Node、Nginx、PM2、SSL 等）。
- **适合**：希望数据完全可控、能接受简单运维的情况。

### 方案 B：PaaS（Railway / Render / Fly.io 等）

- **做法**：把项目（或至少 backend）部署到 PaaS，由平台负责运行 Node、提供公网 URL；前端可同机托管或放在 Vercel/Netlify。
- **数据库**：若平台提供**持久化卷（Volume）**，可继续用 SQLite，把 `backend/data` 挂到卷上；否则需要考虑改用平台提供的 MySQL/PostgreSQL 等（需改 `db.js` 和建表）。
- **优点**：不用管服务器，自动 HTTPS，部署简单。
- **缺点**：可能有月费；若用 SQLite 必须确认 Volume 持久化，否则重启会丢数据。
- **适合**：不想管服务器、能接受一定费用或改用云数据库的情况。

### 方案 C：前端静态托管 + 后端单独部署

- **做法**：前端 `npm run build` 后部署到 Vercel/Netlify/Cloudflare Pages；后端单独部署到 VPS 或 PaaS，并配置 CORS 允许前端域名。
- **数据库**：同方案 A 或 B，取决于后端部署在哪里。
- **优点**：前端走 CDN，访问快；前后端可独立扩展。
- **缺点**：要配置两处、配置 CORS 和前端里的 API 基地址（若前后端不同域）。
- **适合**：对访问速度和全球分布有要求时再考虑；当前项目用方案 A 更简单。

---

## 四、最推荐：方案 A 部署步骤概要

1. **购买云服务器**  
   1 核 2G 即可，系统选 Ubuntu 22.04 或 CentOS 7+。

2. **服务器环境**  
   - 安装 Node.js（建议 18+）、Nginx、PM2。  
   - 可选：用 `git clone` 拉代码，或在本地打包后 scp 上传。

3. **部署应用**  
   - 在项目根目录：`cd frontend && npm run build`。  
   - 在服务器上：`cd backend && npm install --production && npm start`（或用 `npm run dev` 仅作测试）。  
   - 用 PM2 启动 backend，例如：`pm2 start server.js --name experiment-api`，并设置开机自启。

4. **Nginx 反代 + HTTPS**  
   - 将域名解析到服务器 IP。  
   - Nginx 配置：80/443 指向 `http://127.0.0.1:3000`，并配置 SSL（如 Let’s Encrypt）。  
   - 这样用户访问 `https://你的域名/pre-subject`、`/study1-subject` 等，都会由 Nginx 转到 Node，前端和 `/api` 同域，无需改前端代码。

5. **数据与备份**  
   - `experiment.db` 在服务器上的 `backend/data/`。  
   - 定期用 cron 把 `backend/data/experiment.db` 备份到对象存储或另一台机器即可。

完成后，**对外发放的链接**改为：  
`https://你的域名/pre-subject`、`https://你的域名/study1-subject` 等，各地用户各设备各浏览器都能访问，数据都会写入你这台服务器上的同一份数据库，你照常在管理端保存和查看。

---

## 五、简要对比表

| 项目         | 方案 A：VPS | 方案 B：PaaS | 方案 C：前端静态+后端 |
|--------------|-------------|-------------|------------------------|
| 是否需要云服务器 | 是（一台即可） | 否          | 后端需要（或 PaaS）    |
| 数据库       | 继续 SQLite | SQLite+Volume 或云 DB | 同 A/B |
| 改代码       | 无需        | 视是否换 DB | 若前后端不同域需配 API 基地址 |
| 运维         | 自己维护服务器 | 平台负责   | 前端几乎零运维，后端同 A/B |
| 数据可控性   | 完全在你机器 | 依赖平台   | 取决于后端部署方式     |
| 成本         | 约几十元/月  | 有免费额度或月费 | 前端常免费，后端同 A/B |

---

## 六、总结

- **是的，需要把服务部署到公网**（云服务器或 PaaS），这样链接才能被各地、各设备、各浏览器访问，且数据都进同一份库。  
- **最适合你当前架构的方案**：**单机云服务器（VPS）**，保持现有 Node + Express + sql.js + SQLite 不变，用 Nginx + PM2 部署并开启 HTTPS，即可对外发链接，数据全部落在你这边的 `experiment.db`，你照常保存和查看。  
- 若你完全不想碰服务器，可选用 **Railway 或 Render** 等 PaaS，并确认他们支持 SQLite 持久化卷，再按他们的文档部署 backend 和前端。

如果你选定方案 A，我可以再按你使用的云厂商（阿里云/腾讯云等）写一份更具体的 Nginx 配置和 PM2 启动命令示例，方便你一步步操作。
