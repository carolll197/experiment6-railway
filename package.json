{
  "name": "experiment6-railway",
  "private": true,
  "description": "广告创意生成心理学研究实验平台 - Railway 部署入口",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
